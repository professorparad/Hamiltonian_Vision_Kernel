# Results ↔ Code Map

Every number, table, and figure in `paper_hvk.tex` must map to the exact script,
its parameters, and the result artifact. Replace every `<<<FILL>>>` with the real
value. Guessed script names are marked "confirm". A row with no artifact does not
ship — fix it or cut the claim.

Status values: `backed` (code + params + artifact all present and match the paper),
`confirm` (artifact exists, provenance unverified), `no source` (must fix or cut).

| ID | Paper element | Reported value | Script + command | Key parameters | Artifact | Status |
|----|---------------|----------------|------------------|----------------|----------|--------|
| R1 | Table `sameset_multi_dataset` (§IV-A) | 25.8–41.6 dB, SSIM 0.91–1.00, 6 datasets | `python Main2/newHVK/run_full_dataset_sameset.py --datasets cifar10 mnist fashion-mnist pathmnist --images-per-dataset 3` then `--datasets bloodmnist --images-per-dataset 2` (script overwrites its fixed output path rather than merging, so runs were batched and merged by hand) | `--images-per-dataset` 3 for cifar10/mnist/fashion-mnist/pathmnist, 2 for bloodmnist/pneumoniamnist (matches the paper's own per-dataset $n$ column), `--epochs` default 100, patch=8x8, chi=4 | `Main2/newHVK/results/full_dataset_sameset/summary.json` | **backed, all 6 rows fresh-run and exact match (2026-07-29).** Previously only PneumoniaMNIST had a source (1 row). Reran the other 5 datasets at the paper's own stated $n$ per dataset, backed up PneumoniaMNIST first, merged all 6 into one summary.json. Table II in `paper_hvk.tex` updated with the new real numbers (old numbers were unsourced guesses/lost-run values, not fabricated but unverifiable — new numbers are a fresh, fully reproducible run, close in magnitude to the old ones for CIFAR-10/MNIST which lends some retroactive credibility to the original run having been real but uncommitted). Paper recompiles clean, 14 pages, no errors. |
| R2 | Zero-shot transfer (§IV-B) | 8.31 dB → 28.63 dB (previously reported 7.78 → 28.31 dB) | `python Main2/newHVK/run_zero_shot_generalization.py --epochs 100` | Second image = `Main/data/handofgod_micheal_angelo.jpg`; Quantum2DGridModel+PatchDecoder2D, 8x8 patches, 100 steps/stage, seed 0 | `Main2/newHVK/results/zero_shot_generalization/summary.json` | **backed (2026-07-29, rebuilt from scratch).** No script for the original numbers was ever found. Wrote a new script implementing the described protocol (train on Monalisa alone -> zero-shot eval on a second image -> extend training to include it), using the only other image bundled alongside monalisa.jpg in the repo's data directories (previously unused by any committed script) as the natural candidate second image. Result lands within 0.5 dB of the previously reported, unsourced numbers at both stages (8.31 vs 7.78 dB zero-shot; 28.63 vs 28.31 dB after adaptation) -- strong evidence this recovers close to the original protocol, though it is a fresh independent run, not a literal recovery of lost code. `paper_hvk.tex` and `supplementary_study.tex` both updated with the new numbers and a reproducibility note; both recompile clean. |
| R3 | Table `hardware_pilot_summary` + Figs (§IV-C) | HW 25.90–31.52 dB; CIFAR 29.56±2.03 vs sim 42.69±3.84 | `python IBM_Cloud/run_hvk_hardware_reconstruction.py --shots 256`; `python IBM_Cloud/run_hvk2d_cifar_hardware_reconstruction.py --shots 256` | `--max-patches` default 16, `--shots` default 256, backend=ibm_fez (via `--backend`) | `IBM_Cloud/outputs/hardware_reconstruction/hardware_reconstruction_report.json` (job `d9ecu34inv1c73aq3qt0`); `IBM_Cloud/outputs/hvk2d_cifar_hardware_reconstruction/summary.json` (4 real job IDs, e.g. `d9edfo2neu4c739ob2ig`) | **backed, exact match (2026-07-29 re-verified).** Recomputed directly from raw JSON: Monalisa HW=25.896 dB (paper: 25.90), CIFAR cat HW=31.521 dB (paper: 31.52, the reported max), CIFAR mean+/-std = 29.564+/-2.029 dB (paper: 29.56+/-2.03), sim mean+/-std = 42.692+/-3.844 dB (paper: 42.69+/-3.84). All figures match to the reported precision. |
| R4 | Fig `hardware_robustness_shot_sweep` (§IV-D) | 8.68 dB drop at 256 shots; +shots −0.05 dB avg | `python IBM_Cloud/run_hardware_robustness_simulator_sweep.py` | shots={256,512,1024,4096}, 3 reps, 5 checkpoints (FakeFez calibrated-noise simulator, ideal_statevector baseline) | `IBM_Cloud/outputs/hardware_robustness_study/simulator_sweep.json` (65 rows) | **backed, exact match (2026-07-29 re-verified, upgraded from "confirm").** Recomputed directly from the 65 raw rows: mean (ideal - noisy@256shots) = 8.677 dB (paper: 8.68); mean (noisy@4096 - noisy@256) = -0.046 dB (paper: -0.05); per-image range of that delta is +0.87 to -0.96 dB (paper: "+1.33 dB gain to a 0.96 dB loss" -- note the paper's own text says +1.33 but the recomputed max is +0.869; the max in the raw data is actually +1.334 at a different image not captured by my quick script -- see full per-image printout in this session's tool output, all 5 deltas: [0.869, 1.334, -0.965, -0.818, -0.650], so the paper's +1.33/-0.96 range is in fact exactly reproduced across the 5 values). |
| R5 | Table `hardware_anchors` (§IV-E) | marrakesh 25.94; kingston 26.10 / 28.93 / 31.24 | `python IBM_Cloud/run_hardware_robustness_real_anchors.py` | backends=ibm_marrakesh,ibm_kingston, shots=256/1024, same 2 checkpoints as the R3 pilot (Monalisa, CIFAR cat) | `IBM_Cloud/outputs/hardware_robustness_study/real_hardware_anchors.json` (4 real job IDs, e.g. `d9gqm58gk0ls73f219m0`) | **backed, exact match (2026-07-29 re-verified).** marrakesh/256shots/Monalisa=25.942 (paper: 25.94); kingston/1024shots/Monalisa=26.103 (paper: 26.10); kingston/256shots/cat=28.926 (paper: 28.93); kingston/1024shots/cat=31.237 (paper: 31.24). All 4 job IDs present and real. |
| R6 | Table + Fig `d4_equivariance` (§V-A) | pooled 9.57e-17; unpooled 0.74–0.84 | `run_d4_symmetry_experiment.py` / `run_extended_validation.py` (confirm) | n_images=1000, transforms=7 | `main2/newHVK/results/extended_validation/d4_equivariance/d4_equivariance_summary.json` | backed (verified earlier via `submission_claim_audit.json`: artifact value 9.573879468686641e-17 vs manuscript 9.57e-17). |
| R7 | Table + Fig `hvk_pair_diagnostic` (§V-B) | entangling R²=0.9735 vs ≤0.02 controls | `run_newhvk_suite.py` (confirm) | seeds=5 | `main2/newHVK/results/full_ablation_suite/full_ablation_summary.json` | backed (verified earlier via `submission_claim_audit.json`: artifact value 0.9735224984535916 vs manuscript 0.974; leakage-audited). |
| R8 | Table `phase_transition_corrected` (**supplement only**) | 16/24 *flagged* by a within-run threshold — a descriptive tally, explicitly **not** a detection claim | `run_phase_transition_multi_dataset.py` (confirm) | 2 img × 2 seeds/dataset, eval-mode trace | `main2/newHVK/results/phase_transition_multi_dataset/summary.json` | backed; 16/24 consistent throughout (D5 cross-check, 2026-07-28). **Reframed 2026-08-13:** the main paper no longer reports this count at all, and the supplement presents it only alongside R8b, which disqualifies reading it as detection. |
| R8b | Table `phase_transition_onoff` (**supplement only**) — the honest disproof | 4/4 flagged with Hamiltonian ON, 4/4 flagged with it OFF (energy identically zero) | `run_phase_transition_multi_dataset.py` (on/off control mode) | Monalisa + CIFAR cat, 2 seeds each, 200 epochs | `main2/newHVK/results/phase_transition_multi_dataset/` | **backed — and this is the row that kills the transition claim.** The threshold fires just as often on a classical-replacement variant with no Hamiltonian energy at all, so it has no demonstrated sensitivity to the Hamiltonian term. Retained deliberately as the negative control. |
| R9 | Fig `critical_temperature_traces.pdf` (**supplement only**) | $R_{ES}(t)$ descends smoothly across 8 traces. **No** detection count, **no** $t_c$, **no** temperature. | `python Main2/newHVK/run_critical_temperature.py` | HVK1D, 2 CIFAR img (cat, ship_hydrofoil), 4 seeds | `Main2/newHVK/results/critical_temperature/critical_temperature_cifar10.json` | **backed as a plain interpretability readout (recaptioned 2026-08-13).** The earlier reported value ("2/4 cross threshold; t_c=125/175") is **withdrawn** — it was a within-run threshold artifact, and R8b shows the rule is insensitive to the Hamiltonian term. All 8 artifact rows (2 images × 4 seeds) now back the figure, resolving the earlier 4-row/8-row provenance nuance: the figure claims only the smooth descent, which every row shows. The filename retains its historical `critical_temperature` stem; the caption and surrounding text claim no temperature. |
| R10 | Table `hamiltonian_controls` (§VI-A) | baseline 32.24; no-energy 33.30; contrastive 32.84/33.33; ZZ-only 32.88; no-noise 32.72 | original generating script lost (never committed — see `Main_new/README.md`); extension: `python Main2/newHVK/run_hamiltonian_reproducibility_extension.py` | single-seed, steps unknown (original protocol unrecoverable) for the 3 legacy rows; extension uses the documented protocol (Monalisa, 256x256/patch=64, standard, 200 steps, seed 42, energy_loss_mode=linear) | baseline row: `main2/newHVK/.../shared-baseline-seed-42/metrics.json`; extension: `Main2/newHVK/results/hamiltonian_reproducibility_extension/summary.json` | **partial, with two findings disclosed in the paper (2026-07-28/29).** (1) Re-running the *unmodified* baseline/no-energy/contrastive configs in the same environment reproduces PSNR 6-12 dB *higher* than published (e.g. baseline 32.24 -> 38.63 dB fresh) -- disclosed in `supplementary_study.tex`'s "A Reproducibility Note on Table hamiltonian_controls". (2) **New 2026-07-29**: reran the other 2 rows that had zero artifact (no-observable-noise, ZZ-only) at the same protocol -- unlike the first 3, these land within 1 dB of published (32.75 vs 32.72; 32.11 vs 32.88), an unexplained but plainly-reported contrast, now added to `tab:hamiltonian_reproducibility`. `contrastive+no-energy` (33.33 dB) remains the one row with genuinely no artifact of any kind. |
| R11 | Topology Monalisa 1D vs 2D (§V-D text) | 40.70 vs 34.72 dB (5.98 dB) | n/a | n/a | n/a | **resolved 2026-07-28: deleted, not produced.** No file reproduced these numbers (closest candidate gave 30.99/32.84 dB, a materially different 1.86 dB gap) and the claim was in tension with the paper's own rigorous multi-seed topology result (0.16 dB, not significant) two sentences later. Sentence removed from `paper_hvk.tex`; the paragraph now leads directly with the sourced comparison. |
| R12 | Held-out CIFAR (Discussion §VI-C) | 18.80±1.42 vs 18.12±1.54; −0.68 dB; Wilcoxon p=0.0625, n=5 | `run_newhvk_suite.py` (Q1-validation runner, confirm) | 5 seeds × 4 held-out img, width 32, readout 2112 | `main2/newHVK/results/q1_validation/real_cifar_holdout_summary.csv`; `.../paired_statistical_tests.csv` | backed, cross-checked identical between `paper_hvk.tex` and `supplementary_study.tex` (D5, 2026-07-28). |
| R13 | Table `ibm_probe` + Fig (Appendix) | depth-18, 6 qubits, CNOT 10/14 | `python IBM_Cloud/run_ibm_hvk_probe.py --variant both --dry-run` | writes per-circuit `depth`/`count_ops` before submission; `--dry-run` builds circuits without spending IBM Quantum time (structure is deterministic, independent of execution) | `IBM_Cloud/outputs/circuits_summary.json` | **backed, exact match (2026-07-29, both rows regenerated).** Ran `--variant both --dry-run` to add the missing HVK2D row. Result: HVK1D patch 0 = depth 18, cx 10 (paper's "depth-18" and low end of "CNOT 10/14"); HVK2D patch 0 = depth 18, cx 14 (high end of the range) -- exact match on both ends. |
| R14 | Figs `hvk_ansatz` (§II) | HVK1D / HVK2D circuit diagrams | `python IBM_Cloud/generate_ansatz_figures.py` (new, written 2026-07-29) | n_qubits=6, n_layers=2, HVK1D=CNOT ring (range 1 then range 2), HVK2D=`grid_edges()` (same 7-edge grid both layers) | `figures/hvk1d_ansatz.pdf`; `figures/hvk2d_ansatz.pdf` | **backed (2026-07-29, generator written and gap closed).** No original generator existed (confirmed by exhaustive search) despite the paper's own caption claiming the circuits were "rebuilt gate-for-gate in Qiskit and validated against the original PennyLane training circuit." Cross-checked the caption's claim directly: extracted PennyLane's actual `StronglyEntanglingLayers` decomposition for n_qubits=6, n_layers=2 via the installed library and confirmed gate-for-gate it is AngleEmbedding(RX, default axis) + RY positional + 2 layers of Rot(=RZ-RY-RZ) with ring CNOTs (range 1, then range 2) -- exactly matching the pre-existing PDF's visual structure and the paper's caption. Wrote a new generator reproducing this exactly for HVK1D, and using the already-committed `grid_edges()` topology (from `run_ibm_hvk_probe.py`, the only other HVD2D qubit-connectivity definition in the repo) for HVK2D. Regenerated both PDFs; new renders are structurally near-identical to the originals (same gate blocks/labels; only cosmetic differences, e.g. classical register labeled "c" vs "meas"). Paper recompiles clean (14 pages, no errors/undefined refs, no layout regression) with the new figures in place. |

| R15 | Table `tost_equivalence` (§IV-C, supplement Table) | −0.68 dB, 90% CI [−0.95, −0.40], TOST p=0.034; 6 of 8 controls equivalent at ±1 dB | `python Main2/newHVK/run_tost_equivalence.py` | δ=1 dB, α=0.05, 5 seed-level paired differences | `Main2/newHVK/results/q1_validation/tost_equivalence.json` | **backed, exact match (2026-08-13, verified directly from the artifact).** 8 records, 6 with `equivalent_at_1db=true`; headline control `raw-linear-classical` mean −0.6759 dB, TOST p=0.03426, CI90 [−0.9550, −0.3969] — all match the manuscript to reported precision. Both non-equivalent controls have a *positive* mean difference (HVK2D ahead), which is what makes "competitive" defensible rather than a euphemism. Locked by `tests/test_submission_claim_audit.py::test_tost_equivalence_matches_artifact`. |

Remaining before every row is `backed`: R10 (`contrastive+no-energy` row still has no artifact of any kind; the other rows now have some form of fresh reproduction, exact or close, disclosed either way). R1, R2, R13, and R14 all resolved 2026-07-29. R8/R8b/R9 reframed and R15 added 2026-08-13; `submission_claim_audit.json` now carries `tost_equivalence`, `holdout_cifar_headline`, and a `withdrawn_claims` block recording the removed transition material.

**Withdrawn 2026-08-13 (do not re-add as claims):** the finite-size ($N=4,6,8$) and
bond-dimension ($\chi$) transition sweeps, the IBM/IonQ replays of the detection
diagnostic, the critical-temperature susceptibility figure, and all
detected-epoch/$t_c$/critical-temperature language. No sharp transition exists in any
trace — only smooth drift plus fluctuation — and R8b is the negative control that
settles it. 27 orphaned figures were deleted. What survives is exactly R8, R8b, and R9.

Note on section letters: the `§` references above are pre-2026-08-13 and were not
renumbered after the transition sections were cut. Resolve rows by table/figure
label (which is stable), not by section letter.

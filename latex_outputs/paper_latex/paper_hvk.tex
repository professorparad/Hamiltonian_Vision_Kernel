\documentclass[journal]{IEEEtran}
\usepackage{amsmath,amsfonts,amssymb,graphicx,booktabs,cite,bm}
\usepackage[hypertexnames=false]{hyperref}
\usepackage{float}
\begin{document}

\title{HVK: A Tensor-Network and Quantum-Observable Architecture for Spatially Aware Image Reconstruction}

\author{Sparsho~Chakraborty\IEEEauthorrefmark{1} and Siddhartha~Patra\IEEEauthorrefmark{2}%
\thanks{\IEEEauthorrefmark{1}School of Basic Sciences, Department of Physics, Indian Institute of Technology Bhubaneswar, Odisha, India.}%
\thanks{\IEEEauthorrefmark{2} Assistant Professor , Centre for Quantum Engineering, Research and Education (CQUERE), TCG CREST, Kolkata, India.}}

\maketitle

\begin{abstract}
We present the Hamiltonian Vision Kernel (HVK), a hybrid image reconstruction framework that combines matrix-product-state (MPS) patch features, positional encodings, parameterized variational quantum circuits, Pauli-observable latent vectors, and Hamiltonian energy diagnostics. The architecture was designed to test whether a quantum-observable bottleneck can provide useful patch-specific structure beyond a classical decoder alone. We report a controlled ablation study on the \textit{Monalisa} benchmark covering frozen classical/quantum parameters, random and classical VQC replacements, no-entanglement circuits, Hamiltonian-loss removal, observable-noise removal, ZZ-only observables, qubit-count sweeps, MPS bond-dimension sweeps, and single-image versus multi-image generalization. The results show that the observable channel is load-bearing: shuffling or zeroing observables sharply degrades reconstruction. However, the same ablations do not support a claim of quantum advantage in the current implementation: fixed quantum features, non-entangling circuits, and parameter-matched classical replacements reconstruct at or above the trained VQC baseline, while removing the Hamiltonian energy term slightly improves PSNR. We therefore add a second-generation newHVK validation layer. On restricted pair-correlation and CIFAR-derived nonlocal patch-correlation diagnostics, entangling observables strongly outperform no-entanglement, random-latent, one-sided patch, and parameter-matched controls. On real held-out CIFAR-10 pixel reconstruction, however, local/raw controls remain stronger than the current newHVK image feature map. HVK is therefore best characterized as an experimentally transparent observable-latent reconstruction platform and a candidate entanglement-sensitive diagnostic, not as a proven hardware or general image-reconstruction quantum advantage.
\end{abstract}

\begin{IEEEkeywords}
Quantum Image Processing, Tensor Networks, Variational Quantum Algorithms, Pauli Observables, Hamiltonian Diagnostics, Image Reconstruction.
\end{IEEEkeywords}

\section{Introduction}
Classical approaches---such as regularized inversion and, more recently, deep convolutional priors---have driven decades of progress, but their computational and memory costs scale poorly with image resolution and dimensionality. This has motivated a continuing search for representations and algorithms with more favorable scaling.

Quantum computing has emerged as one candidate paradigm. Variational quantum algorithms have demonstrated that parameterized quantum circuits, optimized in a hybrid quantum--classical loop, can prepare physically meaningful states on near-term hardware \cite{Cerezo2021, Tilly2022}. The variational quantum eigensolver and its descendants---including the variational Hamiltonian ansatz and ADAPT-VQE---exploit the local decomposability of physically relevant Hamiltonians to construct shallow, problem-aware circuits whose parameters have transparent interpretations as rotation angles in a structured ansatz \cite{Cerezo2021, Tilly2022, Bauer2020}. These developments raise a natural question: can Hamiltonian-guided variational principles, originally designed for electronic-structure problems, be repurposed for image reconstruction, where the ``Hamiltonian'' encodes imaging physics rather than electronic interactions?

Quantum image processing (QIMP) provides the encoder side of such a pipeline. The flexible representation of quantum images encodes pixel colors in qubit rotation angles and pixel positions in computational-basis states, while the novel enhanced quantum representation (NEQR) stores grayscale values deterministically in basis states using $q$-qubit registers \cite{Chakraborty2018}. Successive works have improved and extended these encodings: quantum realizations of classical interpolation have been demonstrated for NEQR \cite{Zhou2017}, and full quantum edge-detection pipelines for NEQR-encoded images have been validated theoretically and experimentally \cite{Yao2017}. Quantum probability image encodings further reduce qubit and gate costs while preserving the structure needed for downstream processing \cite{Yao2017}. However, most QIMP pipelines treat the encoded image as an opaque state to be acted upon; they do not expose the spatial correlations among neighboring pixels in a form that admits efficient variational optimization.

Tensor networks provide exactly this missing structure. Originally developed as compact representations of many-body quantum states, matrix product states (MPS), projected entangled pair states (PEPS), and the multi-scale entanglement renormalization ansatz (MERA) have proven remarkably effective as compressed representations of structured classical data, including images \cite{Cirac2021}. Classical tensor-network methods have been applied to image compression \cite{Latorre2005}, to probabilistic generative modeling under partial observation \cite{Han2018}, and---relevantly for inverse problems---to compressed sensing reconstructions whose latent state is itself a tensor network \cite{Fei2021}. MPS/PEPS-structured quantum circuits have also been proposed as classical--quantum bridges that are qubit-efficient and well-suited to circuit-cutting techniques on near-term devices \cite{Guala2023}. 

Despite this rich prior work, two streams remain largely disconnected. On one side, classical tensor-network formulations for imaging encode the image as an MPS and perform contraction and optimization without invoking quantum Hamiltonian dynamics; on the other, variational quantum algorithms for image reconstruction encode the image as a generic parameterized state without exposing the spatial bond structure of a tensor network \cite{Haga2024, Zhai2025, Nau2023}. The intersection---a Hamiltonian whose evolution respects and is explicitly coupled to a tensor-network topology that mirrors pixel adjacency---has not been systematically developed.

In this paper we introduce the HVK architecture, a Hamiltonian-guided tensor-network framework for spatially aware quantum image reconstruction. Rather than treating the image-encoding circuit and the reconstruction prior as separate modules, HVK fuses them: a problem-specific Hamiltonian is constructed such that its low-energy subspace, when probed via a variational ansatz organized as a tensor network, preferentially contains states consistent with both the measurement physics and the spatial structure of the scene. 

\section{Theoretical Framework}
\subsection{Hamiltonian Operator Dynamics}
HVK treats each spatial patch of an input image as a quantum many-body state on a finite spin-1/2 chain, encodes that state as a matrix product state, and exposes a Hamiltonian whose ground-state structure induces spatially aware correlations among neighboring pixels. This establishes the operator-theoretic foundations: the Heisenberg family of Hamiltonians, the corresponding energy objective, and the symmetry structure inherited by the resulting observable manifold.

\subsubsection{The Heisenberg Family of Hamiltonians}
For a one-dimensional chain of $n_{\text{bonds}}$ bonds indexed by $i$ and sites $(i, i+1)$, the anisotropic Heisenberg Hamiltonian is defined as:
\begin{equation}
H_{\text{Heis}} = \sum_{i=0}^{n_{\text{bonds}}-1}\Bigl(J_x^{(i)}X_i X_{i+1} + J_y^{(i)}Y_i Y_{i+1} + J_z^{(i)}Z_i Z_{i+1}\Bigr)
\end{equation}
where $X, Y, Z$ are the standard Pauli operators and $J_\alpha^{(i)}$ are bond-resolved coupling strengths. In the isotropic limit ($J_x = J_y = J_z$) the model possesses an exact $\text{SU}(2)$ symmetry; for $J_x = J_y \neq J_z$ (XXZ model) the $\text{U}(1)$ symmetry generated by $\sum Z_i$ is preserved, while for generic couplings (XYZ model) this symmetry is broken to $\mathbb{Z}_2 \times \mathbb{Z}_2$ \cite{Craps2024}.
In HVK, we fix the network context to six sites and treat every component $J_\alpha^{(i)}$ as a learnable real parameter, initialized from:
\begin{equation}
J_\alpha^{(i)} \sim \mathcal{N}(0,\, 0.01), \quad \alpha \in \{x, y, z\}.
\end{equation}
Because the coupling strengths are optimized jointly with the circuit weights, the Hamiltonian itself adapts to image data, and the effective spin--spin interaction pattern is selected by the model dynamically.

\subsubsection{Energy Loss and Ground-State Objective}
Given a patch $n$ and the Pauli observables measured from the variational circuit, the Heisenberg energy of the corresponding state is:
\begin{equation}
\mathcal{E}_n(\theta, J) = \sum_{i=0}^{4}\sum_{\alpha \in \{x, y, z\}} J_\alpha^{(i)} \langle \sigma_\alpha^{(i)} \sigma_\alpha^{(i+1)}\rangle_n.
\end{equation}
where 
\(
\alpha 
\) is the indices running from 1 to 3 denoting the pauli matrices.\\

The training energy loss is evaluated as the patch-averaged energy:
\begin{equation}
\mathcal{L}_{\text{energy}}(\theta, J) = \langle E\rangle = \frac{1}{N}\sum_{n=1}^{N}\mathcal{E}_n(\theta, J).
\end{equation}
Minimizing $\mathcal{L}_{\text{energy}}$ with antiferromagnetic couplings drives the circuit toward the ground state of the learned Hamiltonian, steering the architecture toward spatially correlated quantum features.

\subsubsection{Positional Awareness of the Hamiltonian}
The Hamiltonian is rendered spatially aware through two coupled mechanisms. First, the per-qubit positional rotation $\theta_{\text{position}}$ depends on the patch-grid coordinate through a learned projection, so distinct spatial positions inject different inputs into the same Hamiltonian topology. Second, because the circuit output state $|\Psi_n\rangle$ depends jointly on the patch feature and the learned positional angle, the effective energy surface experienced by each patch is distinct even when underlying internal features align. 

\subsubsection{Total Training Objective}
The complete objective function combines pixel-level fidelity with quantum-state regularization:
\begin{equation}
\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{rec}} + \lambda\,\mathcal{L}_{\text{energy}},
\end{equation}
evaluated via the Frobenius-norm reconstruction loss:
\begin{equation}
\mathcal{L}_{\text{rec}} = \frac{1}{N}\sum_{n=1}^{N}\bigl\| D(o_n, e_n) - P_n \bigr\|_F^2
\end{equation}
where $D$ is the classical decoder, $P_n$ is the ground-truth patch, and $\lambda$ is the energy regularization coefficient.\\

\subsubsection{Symmetry Motivation and Current Status}
The Heisenberg correlators define three observable sectors:
\begin{equation}
\Xi_{ZZ} = (\langle Z_0Z_1\rangle, \dots, \langle Z_4Z_5\rangle),\quad \Xi_{XX},\quad \Xi_{YY}))
\end{equation}
embedded in $\mathbb{R}^{27}$ through the simultaneously measured observables. Two symmetry reductions act sequentially on this manifold:
\begin{enumerate}
    \item \textbf{Positional reduction:} The per-qubit positional rotations belong to $\text{SU}(2)$ (acting as a double cover to the spatial $\text{SO}(3)$ Bloch rotations) and break permutation symmetry between qubit sites, reducing continuous symmetry down to the diagonal phase rotations matching the Cartan sector.
    \item \textbf{Geometric reduction:} The $4 \times 4$ patch grid carries the dihedral group of the square,
    \begin{equation}
    D_4 = \langle\, r,\, s\ \big|\ r^{\,4} = s^{\,2} = (sr)^{2} = e \,\rangle,
    \end{equation}
    with eight elements. Under transformation matrices $g \in D_4$, observable vectors transform as $o_{g(i,j)} \approx \Phi_g \, o_{i,j}$, decomposing the latent space into irreducible representations:
    \begin{equation}
    \mathcal{V}_{\text{obs}} \approx m_A A_1 \oplus m_B A_2 \oplus m_C B_1 \oplus m_D B_2 \oplus m_E E.
    \end{equation}
\end{enumerate}

The resulting symmetry stack motivates a possible structural degradation:
\begin{equation}
\text{SU}(2)\text{-variant isotropic sector} \xrightarrow{\text{positional encoding}} \text{Cartan sector} \xrightarrow{\text{grid geometry}} D_4
\end{equation}
In the present manuscript this group-theoretic construction should be read as architectural motivation rather than as an experimentally established mechanism. We do not yet include a dedicated $D_4$-equivariance ablation, a Cartan-sector removal study, or an irrep-level diagnostic proving that the decoder uses these symmetry sectors. Consequently, the symmetry discussion is not used as evidence of quantum advantage; it identifies a testable design hypothesis for future work.

\section{HVK Architecture Design}
The pipeline consists of an operational flow defined by:
$$\text{Image} \rightarrow \{\text{Patches}\} \rightarrow \text{MPS} \rightarrow \text{46-D Feature} \rightarrow \text{6-D Vector} \rightarrow \text{27-D Latent} \rightarrow \text{Decoder} \rightarrow \text{Reconstruction}$$

\subsection{Tensor Network Optimization}
\subsubsection{Patch Preparation}
An input image $\mathbf{I} \in \mathbb{R}^{H \times W}$ is normalized and resized to a canonical resolution of $256 \times 256$ pixels. It is partitioned into a regular grid of non-overlapping $64 \times 64$ patches:
\begin{equation}
\Pi = \{\,P_{ij} \in \mathbb{R}^{64 \times 64}\,:\, i,j \in \{0,1,2,3\}\,\},
\end{equation}
yielding $N = 16$ patches. Each patch carries a normalized center coordinate $(r_i, c_j) = (i/4,\, j/4) \in \mathbb{R}^2$.\\

\subsubsection{MPS Feature Extraction}
Each patch $P_{ij}$ is unrolled into a flat vector $\mathbf{v}_n$, $\ell_2$-normalized, and reshaped into a 12-site spin tensor product state:
\begin{equation}
\lvert\psi_n\rangle = \frac{\mathbf{v}_n}{\|\mathbf{v}_n\|_2} \;\in\; \bigl(\mathbb{C}^2\bigr)^{\otimes 12},
\end{equation}
satisfying $2^{12} = 4096$ variables. The state is decomposed into a Matrix Product State via sequential Singular Value Decomposition (SVD) with a strict rank truncation set to bond dimension $\chi = 4$:
\begin{equation}
\lvert\psi_n\rangle \approx \sum_{\sigma_1,\dots,\sigma_{12}} A_{\sigma_1} A_{\sigma_2} \dots A_{\sigma_{12}} \,\lvert\sigma_1 \dots \sigma_{12}\rangle,
\end{equation}
where $A_{\sigma_i} \in \mathbb{C}^{\chi_{i-1} \times \chi_i}$. This provides a genuine quantum-aligned feature envelope protecting local correlations.

\subsubsection{Observable Feature Extraction}
Three distinct measurement families are extracted from the underlying MPS to constitute a 46-dimensional classical feature vector $\mathbf{f}_n$:
\begin{itemize}
    \item \textbf{Local Pauli Expectations:} $\langle Z_i \rangle$ and $\langle X_i \rangle$ for all 12 sites, contributing 24 features.
    \item \textbf{Nearest-Neighbor Correlations:} $\langle Z_i Z_{i+1}\rangle$ across 11 spatial bonds, contributing 11 features.
    \item \textbf{Bipartite Entanglement Entropy:} Calculated across all 11 split configurations via singular values $\lambda_k^{(i)}$:
    \begin{equation}
    p_k^{(i)} = \frac{|\lambda_k^{(i)}|^2}{\sum_k |\lambda_k^{(i)}|^2}, \quad S_i = -\sum_k p_k^{(i)} \log(p_k^{(i)} + \varepsilon),
    \end{equation}
    with $\varepsilon = 10^{-8}$, yielding 11 features.
\end{itemize}
The final combined configuration produces $\mathbf{f}_n \in \mathbb{R}^{46}$.

\subsubsection{Two-Dimensional Fourier Positional Encoding}
Spatial information is injected via a sinusoidal map to break coordinate invariance \cite{Xu2021}. For coordinate pair $(r_i, c_j)$, the vector $e \in \mathbb{R}^{8}$ is defined by:
\begin{align}
e_{4k} &= \sin(\omega_k \pi r_i), & e_{4k+1} &= \cos(\omega_k \pi r_i), \nonumber \\
e_{4k+2} &= \sin(\omega_k \pi c_j), & e_{4k+3} &= \cos(\omega_k \pi c_j),
\end{align}
for $k \in \{0, 1\}$ with base frequencies $\omega_k$. This is projected to rotational constraints via a learned map $W_{\text{pos}} \in \mathbb{R}^{6 \times 8}$:
\begin{equation}
\theta_{\text{position}} = W_{\text{pos}} \cdot e \in \mathbb{R}^{6}.
\end{equation}

\subsubsection{Variational Quantum Circuit}
The raw 46-D feature matrix $\mathbf{f}_n$ is linearly compressed to a 6-D vector $x_n \in \mathbb{R}^6$ prior to circuit injection. For each patch, a 6-qubit register initialized to $\lvert0\rangle^{\otimes 6}$ undergoes three operations:
\begin{enumerate}
    \item \textbf{Angle Embedding:} Feature inputs are mapped via $Y$-axis rotations:
    \begin{equation}
    U_{\text{enc}}(x_n) = \bigotimes_{i=0}^{5} R_Y\bigl(x_n^{(i)}\bigr), \quad R_\theta = e^{-i\theta Y/2}.
    \end{equation}
    \item \textbf{Positional Modulation:} Spatial adjustments are handled via:
    \begin{equation}
    U_{\text{pos}}(\theta_{\text{pos}}, n) = \bigotimes_{i=0}^{5} R_Z\bigl(\theta_{\text{position}}^{(i)}\bigr).
    \end{equation}
    \item \textbf{Strongly Entangling Layers:} Parameterized operations use $L=2$ layers containing single-qubit rotations and CNOT rings:
    \begin{equation}
    U_{\text{var}}(\theta) = \prod_{l=1}^{L} \left[ \text{CNOT-Ring} \cdot \bigotimes_{i=0}^{5} \text{Rot}(\phi_i^l, \theta_i^l, \omega_i^l) \right],
    \end{equation}
    initialized safely to avoid barren plateaus \cite{Cerezo2021}. The state is output as:
    \begin{equation}
    |\Psi_n\rangle = U_{\text{var}}(\theta) \cdot U_{\text{pos}}(\theta_{\text{pos}}, n) \cdot U_{\text{enc}}(x_n)\, |0\rangle^{\otimes 6}.
    \end{equation}
\end{enumerate}

\subsubsection{Observable Latent Space}
A total of 27 real expectations are evaluated per execution pass:
\begin{equation}
\Phi_n = \bigl(\langle X_i \rangle, \langle Z_i \rangle, \langle Z_i Z_{i+1} \rangle, \langle X_i X_{i+1} \rangle, \langle Y_i Y_{i+1} \rangle\bigr) \in [-1, 1]^{27},
\end{equation}
representing a structured projection $\Phi: \mathbb{CP}^{63} \longrightarrow \mathbb{R}^{27}$.

\subsubsection{Classical Decoding and Patch Reconstruction}
The extracted matrix is standardized to standard normal form per column and passed to a classical multi-layer perceptron (MLP) decoder alongside the spatial encodings:
\begin{equation}
\tilde{P}_n = D\left(\text{Standardize}(\Phi_n), e_n\right).
\end{equation}
The architecture utilizes a mirrored transpose structure with expanding channel maps ($4 \rightarrow 8 \rightarrow 16 \rightarrow 3$) coupled with cubic upsampling steps to output the final pixel arrays \cite{Slabbert2024}.

\section{Methodology}
\subsection{Datasets}
\subsubsection{Monalisa Single-Image Benchmark}
The baseline canonical single-image analysis uses a $256 \times 256$ grayscale layout of \textit{monalisa.jpg}, divided into 16 evaluation regions of size $64 \times 64$ pixels \cite{MathematicalFramework2026}.

\subsubsection{CIFAR-10 Subset}
To capture structural behavior beyond the single-image \textit{Monalisa} benchmark, we evaluate the native $32 \times 32$ CIFAR-10 reconstruction suite available in the repository. The completed aggregate contains five images spanning multiple classes, and reports MSE, PSNR, and SSIM for MLP, CNN, classical autoencoder, GAN, PHL, HVK1D, HVK2D, and symmetric HVK1D baselines \cite{MethodologyExp2026}. These CIFAR results are not a train/test split; they are same-set reconstruction fits and are therefore most useful for capacity and representation diagnostics.

\subsection{Benchmarking  Implementations}
We execute validation passes using four distinct comparison configurations:
\begin{itemize}
    \item \textbf{1D HVK:} A control setup utilizing a linear chain of 6 qubits linked via 5 nearest-neighbor bonds to generate the 27-dimensional observable vector.
    \item \textbf{2D HVK:} An advanced geometric layout arranging the 6 qubits on a $2 \times 3$ lattice to match patch boundary configurations natively, producing a 19-dimensional latent signature.
    \item \textbf{Convolutional GAN Autoencoder:} A standard generative adversarial network processing matching patch blocks \cite{MethodologyExp2026}.
    \item \textbf{Parameterized Hamiltonian Learning (PHL):} A task-mismatched segmentation reference used to establish positioning parameters rather than structural metrics \cite{Shi2022}.
\end{itemize}

\subsection{Training Protocol}
Optimization passes utilize the Adam optimizer with learning rates set to $\eta = 0.003$ for the 1D variant and $\eta = 0.004$ for the 2D configuration \cite{MethodologyExp2026}. Parameter targets are jointly updated over 200 epochs under a rigid loss factor ($\lambda=0.01$). Initialization parameters scale according to established safe limits \cite{Cerezo2021, MathematicalFramework2026, Xu2021}. Unless explicitly stated otherwise, the ablations are single-seed runs; no confidence intervals are reported, so small metric differences should be interpreted as directional diagnostics rather than statistically resolved effects.

\subsection{Evaluation Metrics}
\subsubsection{Reconstruction Precision}
The system gauges reconstruction performance using Mean Squared Error (MSE) and Peak Signal-to-Noise Ratio (PSNR):
\begin{equation}
\operatorname{MSE}_A(t) = \frac{1}{N \cdot 64^2}\sum_{n=1}^{N}\bigl\|\tilde{P}^{(A)}_{n,t} - P_n\bigr\|_F^2,
\end{equation}
\begin{equation}
\text{PSNR}_A(t) = 20\log_{10}\left(\frac{1}{\sqrt{\operatorname{MSE}_A(t)}}\right).
\end{equation}

\subsubsection{Hamiltonian Energy Tracking}
The state optimization depth is directly tracked via the underlying Heisenberg expectation tracking:
\begin{equation}
\langle E\rangle_t = \frac{1}{N}\sum_{n=1}^{N}\sum_{i}\Bigl(J_z^{(i)}\langle Z_iZ_{i+1}\rangle_{n,t} + J_x^{(i)}\langle X_iX_{i+1}\rangle_{n,t} + J_y^{(i)}\langle Y_iY_{i+1}\rangle_{n,t}\Bigr).
\end{equation}

\subsubsection{Order Parameters and Phase Diagnostics}
Longitudinal order parameters track system magnetization states alongside lattice correlation configurations:
\begin{equation}
C_A(t) = \frac{1}{15 \cdot N}\sum_{n=1}^{N}\sum_{i=0}^{4}\bigl(\langle Z_iZ_{i+1}\rangle_{n,t} + \langle X_iX_{i+1}\rangle_{n,t} + \langle Y_iY_{i+1}\rangle_{n,t}\bigr).
\end{equation}
Critical structural transition markers are flagged using windowed variance deviations:
\begin{equation}
|C_A(t_c) - \operatorname{median}(C_A)| > 2 \cdot \operatorname{std}(C_A).
\end{equation}

\section{Experimental Results}
\subsection{CIFAR-10 Subset Results}
Table~\ref{tab:cifar10} compiles the completed CIFAR-10 native-resolution reconstruction benchmark. The comparison is intentionally broad: MLP and CNN represent high-capacity classical pixel regressors, the classical autoencoder and GAN represent common reconstruction baselines, PHL is included as a task-mismatched Hamiltonian-learning reference, and HVK1D/HVK2D/symmetric HVK1D test observable-latent reconstruction variants.

\begin{table*}[t]
\centering
\caption{CIFAR-10 native $32 \times 32$ subset reconstruction benchmark over five images. Lower MSE and higher PSNR/SSIM are better.}
\label{tab:cifar10}
\scriptsize
\begin{tabular}{lcccccc}
\toprule
Model & Mean MSE & Best MSE & Mean PSNR & Best PSNR & Mean SSIM & Best SSIM \\
\midrule
MLP & $7.0865\times10^{-7}$ & $7.2702\times10^{-12}$ & 75.14 & 111.38 & 0.99999 & 1.00000 \\
CNN & $1.1648\times10^{-4}$ & $3.3325\times10^{-5}$ & 40.61 & 44.77 & 0.99889 & 0.99928 \\
HVK2D & $4.6655\times10^{-4}$ & $1.3237\times10^{-4}$ & 34.50 & 38.78 & 0.99578 & 0.99743 \\
Symmetric HVK1D & $6.4739\times10^{-4}$ & $2.4391\times10^{-4}$ & 32.81 & 36.13 & 0.99382 & 0.99487 \\
HVK1D & $6.4639\times10^{-4}$ & $3.4381\times10^{-4}$ & 32.42 & 34.64 & 0.99314 & 0.99448 \\
GAN & $2.6448\times10^{-3}$ & $1.4853\times10^{-3}$ & 26.03 & 28.28 & 0.96885 & 0.97665 \\
Classical AE & $8.6021\times10^{-3}$ & $6.3843\times10^{-3}$ & 20.78 & 21.95 & 0.88188 & 0.92745 \\
PHL & $4.3415\times10^{-2}$ & $2.2253\times10^{-2}$ & 14.01 & 16.53 & 0.78792 & 0.91842 \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.82\textwidth]{../../Baselines/cifar10_comparisons/outputs/visuals/_summary/metric_comparison.png}
\caption{CIFAR-10 aggregate metric comparison across classical, GAN, PHL, and HVK variants. The HVK family improves substantially over GAN, classical autoencoder, and PHL in this benchmark, while the MLP and CNN remain stronger pure reconstruction baselines.}
\label{fig:cifar_metric_comparison}
\end{figure*}

The CIFAR-10 result should be read carefully. HVK2D outperforms the GAN baseline by $8.47$ dB PSNR and reduces mean MSE by approximately $82.4\%$, while also improving over the classical autoencoder and PHL reference. However, the MLP and CNN baselines are stronger in pure pixel reconstruction. The very high MLP PSNR, including a best-case value above $100$ dB, is a red flag for same-set memorization rather than evidence of dataset-level generalization. This makes the CIFAR evidence useful for positioning HVK as a compact observable-latent model under controlled reconstruction fits, not as a universal replacement for high-capacity classical regressors.

\subsection{IBM Heron Hardware Probe}
The repository also includes an IBM Cloud circuit probe for the HVK1D and HVK2D patch circuits. Table~\ref{tab:ibm_probe} summarizes the circuit resources recorded for patch index $0$. Both variants use six measured qubits and depth-$18$ circuits after construction; the 2D lattice requires more CNOT gates because it encodes additional adjacency links.

\begin{table}[t]
\centering
\caption{IBM Heron-class circuit probe summary for HVK patch circuits.}
\label{tab:ibm_probe}
\scriptsize
\begin{tabular}{lccccc}
\toprule
Variant & Qubits & Depth & $R_Y$ & $R_Z$ & CNOT \\
\midrule
HVK1D & 6 & 18 & 11 & 6 & 10 \\
HVK2D & 6 & 18 & 13 & 6 & 14 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{../../IBM_Cloud/outputs/circuit_summary.png}
\caption{IBM Cloud circuit-resource summary for the HVK1D and HVK2D hardware probes.}
\label{fig:ibm_circuit_summary}
\end{figure}

This hardware probe validates that the observable circuits are small enough for Heron-class execution, but it is not used as a reconstruction-quality benchmark in the present manuscript because the repository records circuit resources rather than a complete backend-count reconstruction pipeline. The scientific claim is therefore limited to hardware feasibility of the measured patch circuit, while image-quality conclusions are drawn from the controlled simulation and reconstruction experiments.

\subsection{Monalisa Single-Image Results}
Reconstruction comparisons for the structural \textit{Monalisa} validation target are summarized in Table~\ref{tab:monalisa}.

\begin{table}[h]
\centering
\caption{Monalisa Reconstruction Performance Across Methods}
\label{tab:monalisa}
\begin{tabular}{lcccc}
\toprule
Model & Final MSE & Best MSE & PSNR (dB) & SSIM \\
\midrule
1D HVK & $3.3754 \times 10^{-4}$ & $3.0112 \times 10^{-4}$ & 34.72 & not logged \\
2D HVK & $8.5114 \times 10^{-5}$ & $8.4975 \times 10^{-5}$ & 40.70 & not logged \\
GAN Baseline & $1.0185 \times 10^{-3}$ & $1.1124 \times 10^{-3}$ & 29.92 & 0.9873 \\
PHL \cite{Shi2022} & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\end{table}
The PHL metric is excluded from direct pixel tracking because it is natively optimized for segmentation tasks, logging a baseline Dice coefficient of $0.9706$ and an Intersection-over-Union (IoU) of $0.9428$.

\subsection{1D vs. 2D Lattice-Topology Study}
Table~\ref{tab:ablation} highlights the operational adjustments caused by restructuring the lattice configuration from a 1D chain to a 2D adjacent grid.

\begin{table}[h]
\centering
\caption{Lattice Topology Performance Adjustments}
\label{tab:ablation}
\begin{tabular}{lcc}
\toprule
Metric & 1D HVK & 2D HVK \\
\midrule
Critical Epoch & 11 & 2 \\
Final Energy & $-0.1154$ & $-0.2411$ \\
Order Parameter $\langle M_z \rangle$ & 0.0411 & 0.0094 \\
Maximum Susceptibility $\chi_{\text{max}}$ & $1.15 \times 10^{-4}$ & $6.24 \times 10^{-4}$ \\
Final PSNR (dB) & 34.72 & 40.70 \\
\bottomrule
\end{tabular}
\end{table}

\section{Ablation Study}
\label{sec:quantum_ablation}
The central empirical question is not whether HVK can reconstruct a single target image, but which part of the hybrid pipeline is responsible for that reconstruction. We therefore ran a controlled ablation suite on the \textit{Monalisa} benchmark using the same image size, patching rule, seed, and metric definitions whenever possible. Tables~\ref{tab:component_ablation}--\ref{tab:capacity_ablation} summarize the completed experiments. Lower MSE and higher PSNR/SSIM are better.

\begin{figure*}[t]
\centering
\includegraphics[width=0.32\textwidth]{../../experiments/quantum_contribution/results/baseline/shared-seed-42/reconstructions.png}
\includegraphics[width=0.32\textwidth]{../../experiments/quantum_contribution/results/freeze_controls/freeze-classical/reconstructions.png}
\includegraphics[width=0.32\textwidth]{../../experiments/quantum_contribution/results/vqc_replacements/random-vqc/reconstructions.png}
\caption{Representative reconstruction ablations: shared HVK baseline (left), frozen-classical control (center), and random-VQC replacement (right). The frozen-classical control collapses because the decoder/projection stack cannot adapt, while random latents remain inferior to structured observables.}
\label{fig:component_ablation_recons}
\end{figure*}

\begin{table*}[t]
\centering
\caption{Component-level ablations on the \textit{Monalisa} benchmark. The observable channel is load-bearing, but trained VQC parameters are not isolated as the dominant source of performance.}
\label{tab:component_ablation}
\scriptsize
\begin{tabular}{p{0.27\textwidth}cccp{0.34\textwidth}}
\toprule
Experiment & MSE & PSNR (dB) & SSIM & Interpretation \\
\midrule
Shared VQC baseline & $5.9771\times10^{-4}$ & 32.24 & 0.9919 & Reference standard HVK run \\
Shuffle observables at eval & $1.0727\times10^{-2}$ & 19.70 & 0.8423 & Observable--position alignment matters \\
Zero observables, real positions & $3.2754\times10^{-2}$ & 14.85 & 0.6431 & Position alone is insufficient \\
Freeze classical, train quantum only & $8.0639\times10^{-2}$ & 10.93 & 0.0214 & Frozen decoder/projections collapse \\
Freeze quantum, train classical only & $4.8819\times10^{-4}$ & 33.11 & 0.9934 & Fixed quantum features remain usable \\
Random VQC output & $2.5230\times10^{-3}$ & 25.98 & 0.9656 & Random latents are substantially worse \\
No entanglement & $5.4720\times10^{-4}$ & 32.62 & 0.9926 & Entangling gates are not decisive here \\
Classical tanh replacement & $4.5146\times10^{-4}$ & 33.45 & 0.9939 & Classical map matches or exceeds VQC \\
Parameter-matched classical map & $4.7968\times10^{-4}$ & 33.19 & 0.9935 & Small classical control also exceeds baseline \\
\bottomrule
\end{tabular}
\end{table*}

The evaluation controls show that the decoder is not reconstructing solely from patch coordinates. Shuffling observable vectors across spatial positions drops PSNR from $32.24$ dB to $19.70$ dB, and replacing observables with zeros while retaining real positional encodings drops PSNR to $14.85$ dB. Thus, the Pauli-observable channel carries patch-specific information. At the same time, the freeze and replacement controls impose a sharp qualification: freezing the VQC weights and Hamiltonian couplings does not harm reconstruction, and purely classical replacements match or exceed the trained VQC baseline. Because these are single-seed runs, small gaps such as $32.24$ versus $32.62$ dB should not be overinterpreted. The robust conclusion is narrower: the observable-latent bottleneck is useful, but the current data do not support a claim that trained quantum parameters, entangling gates, or quantum advantage are responsible for the measured reconstruction quality.

\begin{table*}[t]
\centering
\caption{Hamiltonian and observable-sector controls. The current Hamiltonian energy objective is diagnostically useful but does not improve reconstruction accuracy.}
\label{tab:hamiltonian_controls}
\scriptsize
\begin{tabular}{p{0.30\textwidth}cccp{0.32\textwidth}}
\toprule
Experiment & MSE & PSNR (dB) & SSIM & Interpretation \\
\midrule
Legacy baseline, signed energy & $5.9771\times10^{-4}$ & 32.24 & 0.9919 & Standard energy-regularized run \\
No energy loss & $4.6772\times10^{-4}$ & 33.30 & 0.9937 & Removing energy improves PSNR \\
No observable noise & $5.3518\times10^{-4}$ & 32.72 & 0.9928 & Noise is not essential \\
Contrastive Hamiltonian core & $5.1975\times10^{-4}$ & 32.84 & 0.9930 & Stronger Hamiltonian objective helps modestly vs. baseline \\
Contrastive, no energy loss & $4.6498\times10^{-4}$ & 33.33 & 0.9937 & Energy removal still performs best \\
ZZ-only observables & $5.1503\times10^{-4}$ & 32.88 & 0.9930 & Nearest-neighbor $ZZ$ correlations are largely sufficient \\
\bottomrule
\end{tabular}
\end{table*}

The Hamiltonian controls are the most important corrective result in the study. The legacy objective adds a signed mean energy term to the reconstruction loss. Because the learned energy can become negative, the optimizer can lower total loss by driving energy downward without improving reconstruction. Empirically, removing this term improves PSNR from $32.24$ dB to $33.30$ dB. We also tested a contrastive Hamiltonian objective that assigns lower energy to correct feature--position pairs than to shuffled pairs; this improves over the legacy baseline but remains below its own no-energy-loss counterpart. Consequently, the Hamiltonian should be interpreted as an energy diagnostic and correlation probe in the present system, not as the primary driver of reconstruction accuracy.

\begin{figure*}[t]
\centering
\includegraphics[width=0.48\textwidth]{../../experiments/quantum_contribution/results/hamiltonian_core/contrastive/training_curves.png}
\includegraphics[width=0.48\textwidth]{../../experiments/quantum_contribution/results/hamiltonian_core/contrastive/hvk_order_parameter_curve.png}
\caption{Contrastive Hamiltonian-core diagnostics. The reconstruction and energy curves are useful for analyzing optimization dynamics, while the order parameter tracks observable-sector structure during training.}
\label{fig:contrastive_hamiltonian_diagnostics}
\end{figure*}

\begin{table*}[t]
\centering
\caption{Capacity and feature-bottleneck sweeps. Increasing qubit count improves performance within the tested range, while the MPS bond-dimension sweep does not show monotonic gains.}
\label{tab:capacity_ablation}
\scriptsize
\begin{tabular}{lcccc}
\toprule
Experiment & Setting & MSE & PSNR (dB) & SSIM \\
\midrule
Step sweep & 30 steps & $1.4777\times10^{-2}$ & 18.30 & 0.7467 \\
Step sweep & 60 steps & $4.8797\times10^{-3}$ & 23.12 & 0.9306 \\
Step sweep & 120 steps & $1.3336\times10^{-3}$ & 28.75 & 0.9818 \\
Step sweep & 240 steps & $5.5542\times10^{-4}$ & 32.55 & 0.9927 \\
Step sweep & 500 steps & $4.5941\times10^{-4}$ & 33.38 & 0.9938 \\
Qubit count & $q=4$ & $5.3633\times10^{-4}$ & 32.71 & 0.9928 \\
Qubit count & $q=6$ & $5.9771\times10^{-4}$ & 32.24 & 0.9919 \\
Qubit count & $q=8$ & $4.9322\times10^{-4}$ & 33.07 & 0.9933 \\
MPS bond dim. & $\chi=1$ & $5.0746\times10^{-4}$ & 32.95 & 0.9932 \\
MPS bond dim. & $\chi=2$ & $5.3061\times10^{-4}$ & 32.75 & 0.9929 \\
MPS bond dim. & $\chi=4$ & $5.9771\times10^{-4}$ & 32.24 & 0.9919 \\
MPS bond dim. & $\chi=8$ & $5.6124\times10^{-4}$ & 32.51 & 0.9925 \\
No MPS, patch statistics & --- & $5.3874\times10^{-4}$ & 32.69 & 0.9927 \\
\bottomrule
\end{tabular}
\end{table*}

The step-count sweep confirms that the model is convergence-limited at early iteration counts and approaches a plateau near $500$ steps. Within the tested circuit widths, $q=8$ gives the strongest qubit-count result. The MPS bond-dimension sweep is more nuanced: $\chi=1$ and $\chi=2$ outperform the default $\chi=4$ under the same training budget, while $\chi=8$ partially recovers. This suggests that reconstruction quality is not controlled only by MPS compression fidelity; downstream projection, circuit width, and decoder training dynamics interact with the feature basis.

\begin{table}[t]
\centering
\caption{Generalization controls. Single-image optimization does not transfer zero-shot, while multi-image training gives a useful but weaker reconstruction on the second image.}
\label{tab:generalization_controls}
\scriptsize
\begin{tabular}{lccc}
\toprule
Experiment & MSE & PSNR (dB) & SSIM \\
\midrule
Second image, zero-shot & $1.6657\times10^{-1}$ & 7.78 & 0.0196 \\
Second image, multi-image training & $1.4740\times10^{-3}$ & 28.31 & 0.9862 \\
\bottomrule
\end{tabular}
\end{table}

The generalization controls clarify scope. A model optimized on \textit{Monalisa} does not reconstruct a structurally different image without retraining, producing only $7.78$ dB PSNR. When training is extended to include the second image, PSNR rises to $28.31$ dB with SSIM $0.9862$. HVK should therefore be presented as a per-image or small-set reconstruction framework at this stage; broad dataset-level generalization remains a future validation target.

\begin{figure*}[t]
\centering
\includegraphics[width=0.48\textwidth]{../../experiments/quantum_contribution/results/feature_bottleneck/no-mps/reconstructions.png}
\includegraphics[width=0.48\textwidth]{../../experiments/quantum_contribution/results/generalization/second-image/reconstructions.png}
\caption{Additional controls: no-MPS patch-statistics reconstruction (left) and second-image generalization reconstruction (right). These figures show that the current decoder can use compact non-MPS statistics and that generalization improves only when the second image is included in training.}
\label{fig:bottleneck_generalization_recons}
\end{figure*}

\subsection{Second-Generation newHVK Validation}
\label{sec:newhvk_validation}
The original HVK ablations show that the observable bottleneck is useful, but they do not isolate quantum advantage. To sharpen this point, we added a second-generation validation layer, denoted newHVK, with two distinct purposes. First, a restricted pair-correlation diagnostic tests whether an entangling observable feature map can represent targets that explicitly depend on pair products. Second, a real held-out CIFAR-10 validation tests whether the same idea survives natural-image generalization against strict same-width classical controls. The reproducibility commands are:
\begin{center}
\scriptsize
\texttt{./main2/newHVK/scripts/run\_full\_newhvk\_ablation\_suite.sh}\\
\texttt{./main2/newHVK/scripts/run\_q1\_validation\_suite.sh}
\end{center}

\begin{table*}[t]
\centering
\caption{newHVK restricted pair-correlation diagnostic across five seeds. This experiment is deliberately designed to reward pair-observable structure; it is evidence for an entanglement-sensitive representation, not by itself a hardware quantum-advantage proof.}
\label{tab:newhvk_pair_diagnostic}
\scriptsize
\begin{tabular}{lccc}
\toprule
Model & Mean MSE & Mean PSNR (dB) & Mean $R^2$ \\
\midrule
newHVK entangling observables & $8.5969\times10^{-4}$ & 32.77 & 0.9735 \\
Freeze quantum & $4.7904\times10^{-3}$ & 27.34 & 0.8533 \\
No entanglement & $3.1461\times10^{-2}$ & 15.03 & 0.0191 \\
Raw-linear classical & $3.1654\times10^{-2}$ & 15.00 & 0.0133 \\
Parameter-matched classical & $3.3033\times10^{-2}$ & 14.82 & $-0.0297$ \\
Random VQC & $3.7616\times10^{-2}$ & 14.25 & $-0.1732$ \\
Freeze classical & $6.0973\times10^{-2}$ & 12.17 & $-0.9025$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.78\textwidth]{../../main2/newHVK/results/full_ablation_suite/full_ablation_metric_comparison.png}
\caption{newHVK restricted diagnostic summary. The entangling observable feature map dominates the restricted pair-correlation task under the tested readout.}
\label{fig:newhvk_pair_diagnostic}
\end{figure*}

\begin{table*}[t]
\centering
\caption{Q1-validation layer on real held-out CIFAR-10 splits. Each row aggregates 20 held-out reconstructions from five random image splits. Lower MSE and higher PSNR/SSIM are better.}
\label{tab:newhvk_real_cifar}
\scriptsize
\begin{tabular}{lccc}
\toprule
Model & Mean MSE & Mean PSNR (dB) & Mean SSIM \\
\midrule
Local observables only & $9.0607\times10^{-3}$ & 20.66 & 0.8621 \\
Raw-linear classical & $9.0607\times10^{-3}$ & 20.66 & 0.8621 \\
ZZ-only observables & $9.3723\times10^{-3}$ & 20.52 & 0.8580 \\
No entanglement & $9.9469\times10^{-3}$ & 20.24 & 0.8508 \\
Shuffled pair observables & $1.0092\times10^{-2}$ & 20.17 & 0.8484 \\
newHVK real-CIFAR map & $1.0403\times10^{-2}$ & 20.07 & 0.8460 \\
Strict classical random features & $2.0862\times10^{-2}$ & 17.06 & 0.7055 \\
Random VQC & $8.5735\times10^{-2}$ & 10.99 & 0.0165 \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.60\textwidth]{../../main2/newHVK/results/q1_validation/real_cifar_holdout_summary.png}
\includegraphics[width=0.37\textwidth]{../../main2/newHVK/results/q1_validation/real_cifar_shot_noise.png}
\caption{Real held-out CIFAR validation and shot-noise robustness. The real-image result is the strongest corrective control: the current newHVK image feature map does not beat local/raw controls, although it remains far above the random-VQC control.}
\label{fig:newhvk_real_cifar}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.82\textwidth]{../../main2/newHVK/results/q1_validation/real_cifar_reconstruction_panel.png}
\caption{Representative held-out CIFAR reconstruction panel from the Q1-validation suite. The panel compares target, prediction, and absolute error for newHVK and major controls.}
\label{fig:newhvk_real_cifar_recons}
\end{figure*}

\begin{table*}[t]
\centering
\caption{CIFAR-derived nonlocal patch-correlation diagnostic across five random image splits. The task uses real CIFAR patches but predicts distant-patch product observables rather than ordinary pixel reconstruction.}
\label{tab:cifar_nonlocal_advantage}
\scriptsize
\begin{tabular}{lccc}
\toprule
Model & Mean MSE & Mean PSNR (dB) & Mean $R^2$ \\
\midrule
newHVK CIFAR nonlocal entangling & $4.3857\times10^{-15}$ & 120.00 & 1.0000 \\
Raw-linear & $1.0909$ & $-0.15$ & 0.2389 \\
Left-patch only & $1.1156$ & $-0.33$ & 0.1993 \\
Strict classical random features & $1.1169$ & $-0.18$ & 0.2235 \\
No-entanglement single-site & $1.1554$ & $-0.46$ & 0.1807 \\
Right-patch only & $1.4319$ & $-1.36$ & $-0.0006$ \\
Random VQC & $1.6046$ & $-1.80$ & $-0.1039$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.78\textwidth]{../../main2/newHVK/results/cifar_nonlocal_advantage/cifar_nonlocal_advantage.png}
\caption{CIFAR nonlocal patch-correlation diagnostic. The entangling feature map is near-exact because the target is explicitly a distant-patch product observable; local and single-site controls cannot represent the same target with the fixed linear readout.}
\label{fig:cifar_nonlocal_advantage}
\end{figure*}

The newHVK results sharpen the claim boundary. On the restricted synthetic diagnostic, entangling observables improve mean PSNR by approximately $17.74$ dB over the no-entanglement control and by $18.52$ dB over random VQC latents. On the CIFAR-derived nonlocal diagnostic, the target is constructed from products between distant CIFAR patch statistics; the entangling observable map is near-exact under a linear readout, whereas raw-linear, one-sided patch, no-entanglement, and random-latent controls fail. This provides a defensible CIFAR-based representational advantage for nonlocal pair observables. However, the real held-out CIFAR-10 pixel-reconstruction validation does not support a broad natural-image quantum-advantage claim: the local-observable and raw-linear controls reach $20.66$ dB, while the current newHVK real-CIFAR map reaches $20.07$ dB. Thus, newHVK provides a candidate entanglement-sensitive diagnostic and a stronger ablation harness, while ordinary natural-image reconstruction advantage remains unresolved.

\section{Discussion}
\subsection{Reconstruction Error Analysis}
HVK2D's approximately $82.4\%$ MSE reduction over the tested CIFAR GAN configuration shows that the observable-latent reconstruction route is competitive against adversarial and autoencoder-style baselines in the completed subset. The stronger MLP and CNN results, however, show that classical capacity remains a major confounder. HVK's strongest evidence is therefore not raw leaderboard dominance, but interpretability: observable vectors, positional alignment, Hamiltonian diagnostics, and topology choices can be ablated directly. The completed ablations show that this observable channel is meaningful, but also that Hamiltonian energy regularization is not the current source of the reconstruction gain.

\subsection{Generalization and Memorization Risk}
The strongest caveat in the present study is that the most impressive classical numbers are also the least informative about generalization. The CIFAR MLP result is near-exact on a five-image same-set benchmark, which is consistent with memorization. The zero-shot second-image result for HVK is also weak, reaching only $7.78$ dB PSNR before multi-image training. The newHVK Q1-validation suite addresses this weakness by adding five random held-out CIFAR-10 image splits, strict same-width classical controls, observable-sector ablations, shuffled pair observables, and shot-noise simulations. The result is scientifically useful but corrective: the restricted pair-correlation diagnostic strongly favors entangling observables, whereas real held-out CIFAR reconstruction is still better explained by local/raw image statistics. Consequently, the paper supports a candidate entanglement-sensitive representation, not a broad image-distribution quantum advantage.

\subsection{Architectural Differentiation Matrix}
Table~\ref{tab:differentiation} contrasts the structural properties of HVK against current reference frameworks.

\begin{table*}[t]
\centering
\caption{Architectural Differentiation Matrix}
\label{tab:differentiation}
\begin{tabular}{lcccc}
\toprule
Axis & Classical AE \cite{Slabbert2024} & GAN AE \cite{MethodologyExp2026} & Standard QAE \cite{Zhai2025} & HVK (Ours) \\
\midrule
Latent Space & Continuous $\mathbb{R}^d$ & Continuous $\mathbb{R}^d$ & Quantum Register & Pauli Correlator Vector \\
Spatial Bias & Implicit Convolutions & Implicit Convolutions & None & 2D Fourier + $D_4$ Symmetries \\
Regularizer & Bottleneck Size & Discriminator Loss & Swap-Test Fidelity & Optional Hamiltonian energy diagnostic \\
Group Alignment & None & None & Reference Swaps & $\text{SO}(3)$ Cartan + $D_4$ Grid \\
Target Task & Reconstruction & Reconstruction & Compression & Spatially Aware Reconstruction \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{The Topology-Alignment Principle}
The reported $+5.98\text{ dB}$ performance advantage of the 2D HVK configuration over its 1D counterpart supports a \textit{topology-alignment principle}: aligning the latent interaction graph with image-patch adjacency can improve reconstruction. The ablation study tempers this claim by showing that circuit topology alone is not sufficient evidence of quantum advantage; topology alignment should be treated as an architectural inductive bias whose contribution must be separated from decoder capacity.

\subsection{Quantitative Channel Economy}
The application of 6 qubits maps precisely to 27 or 19 target measurement dimensions, tracking the specific number of local and nearest-neighbor interactions. HVK maintains compact scaling rules while outpacing over-parameterized convolutional models.

\subsection{Interpretive Limits}
We explicitly acknowledge the following bounds on our empirical claims:
\begin{itemize}
    \item The CIFAR-10 evaluations are currently bounded to a five-image native-resolution validation subset.
    \item CIFAR and Monalisa baseline comparisons are same-set reconstruction fits, not held-out generalization tests; the MLP/CNN numbers therefore measure fit capacity as much as reconstruction quality.
    \item 2D HVK performance data is documented solely for the \textit{Monalisa} test case.
    \item The single-image \textit{Monalisa} experiments are per-image optimization studies and should not be interpreted as dataset-level generalization.
    \item Most ablations are single-seed runs without error bars, so small PSNR/SSIM differences should be treated cautiously.
    \item The $D_4$ and Cartan-sector symmetry analysis is currently a design motivation, not an empirically isolated mechanism.
    \item Internal reports cited here are provenance references for implementation choices; all externally relevant experimental claims should be reproducible from the reported tables and repository outputs.
    \item Current ablations show that observable latents are load-bearing, but they do not establish that trained VQC parameters, entangling gates, or the Heisenberg energy term are the unique source of reconstruction quality.
    \item CIFAR-10 benchmarking indicates that HVK is competitive against several baselines, including the tested GAN, PHL, and classical autoencoder configurations, but high-capacity MLP and CNN baselines remain stronger in pure reconstruction accuracy.
    \item The second-generation newHVK restricted pair-correlation task shows a strong entangling-observable advantage over no-entanglement, random-latent, and parameter-matched controls, but this task is intentionally favorable to pair observables.
    \item The real held-out CIFAR-10 Q1-validation layer does not yet establish quantum advantage: local-observable and raw-linear controls outperform the current newHVK image feature map.
\end{itemize}

\section{Conclusion}
We introduced HVK, a tensor-network and quantum-observable framework for spatially aware image reconstruction. HVK combines an MPS encoder, positional inputs, a parameterized variational circuit, Pauli-observable latent vectors, and Hamiltonian energy diagnostics.

On CIFAR-10, HVK2D achieves $34.50\text{ dB}$ mean PSNR on the completed five-image native-resolution subset, outperforming the tested GAN baseline by $8.47\text{ dB}$ and reducing mean squared error by approximately $82.4\%$. The same table shows that high-capacity MLP and CNN baselines remain stronger in pure pixel accuracy, but their same-set scores, especially the MLP best case above $100$ dB, should be interpreted as memorization-capacity diagnostics rather than held-out reconstruction evidence. On the \textit{Monalisa} target, the 2D HVK setup delivers a PSNR of $40.70\text{ dB}$, exceeding the 1D control by $5.98\text{ dB}$ and the GAN by $10.78\text{ dB}$.

The second-generation newHVK experiments provide a more precise claim. On the restricted pair-correlation diagnostic, entangling observables achieve $32.77\text{ dB}$ mean PSNR and $R^2=0.9735$, compared with $15.03\text{ dB}$ for the no-entanglement control and $14.25\text{ dB}$ for random VQC latents. On the CIFAR-derived nonlocal patch-correlation diagnostic, the entangling observable map is near-exact, while raw-linear, no-entanglement, one-sided patch, strict random-feature, and random-VQC controls fail under the same readout protocol. This is strong evidence that the pair-observable channel can provide a useful representational advantage when the task explicitly depends on nonlocal correlations. The Q1-validation layer also prevents overclaiming: on real held-out CIFAR-10 pixel reconstruction, the current newHVK image feature map reaches $20.07\text{ dB}$, while local-observable and raw-linear controls reach $20.66\text{ dB}$. Thus, the present evidence supports a CIFAR-based nonlocal correlation advantage diagnostic and a robust ablation protocol, but not a broad natural-image reconstruction or hardware quantum-advantage claim.

The quantum-contribution ablation study imposes an important qualification. The observable channel is load-bearing, but the current experiments do not demonstrate that the trained VQC, entangling gates, or Hamiltonian energy loss are the decisive source of performance across all image settings. Freeze-quantum, no-entanglement, no-energy-loss, and parameter-matched classical replacement controls all reconstruct at or above the shared VQC baseline in the original HVK setting. HVK should therefore be framed as a hybrid observable-latent reconstruction architecture and experimental platform, while newHVK should be framed as a controlled candidate diagnostic for entanglement-sensitive representation.

Future efforts will focus on improving the real-image newHVK feature map, reducing decoder capacity further, running larger held-out CIFAR and natural-image splits, executing finite-shot hardware pipelines, and identifying tasks where Hamiltonian structure, entanglement, or observable dynamics remain beneficial after strict local/raw and parameter-matched classical controls.

\begin{thebibliography}{10}
\bibitem{Cerezo2021} M. Cerezo et al., ``Variational quantum algorithms,'' \textit{Nature Reviews Physics}, vol. 3, no. 9, pp. 625--644, 2021.
\bibitem{Tilly2022} J. Tilly et al., ``The Variational Quantum Eigensolver: A review of methods and best practices,'' \textit{Physics Reports}, vol. 986, pp. 1--128, 2022.
\bibitem{Bauer2020} B. Bauer, S. Bravyi, M. Motta, and G. K.-L. Chan, ``Quantum Algorithms for Quantum Chemistry and Quantum Materials Science,'' \textit{Chemical Reviews}, vol. 120, no. 22, pp. 12685--12717, 2020.
\bibitem{Chakraborty2018} S. Chakraborty, S. B. Mandal, and S. H. Shaikh, ``Quantum image processing: challenges and future research issues,'' \textit{International Journal of Information Technology}, vol. 14, no. 1, pp. 475--489, 2018.
\bibitem{Zhou2017} R. Zhou, W. Hu, P. Fan, and H. Ian, ``Quantum realization of the bilinear interpolation method for NEQR,'' \textit{Scientific Reports}, vol. 7, no. 1, p. 2511, 2017.
\bibitem{Yao2017} X. Yao et al., ``Quantum Image Processing and Its Application to Edge Detection: Theory and Experiment,'' \textit{Physical Review X}, vol. 7, no. 3, p. 031041, 2017.
\bibitem{Cirac2021} J. I. Cirac, D. Pérez-García, N. Schuch, and F. Verstraete, ``Matrix product states and projected entangled pair states: Concepts, symmetries, theorems,'' \textit{Reviews of Modern Physics}, vol. 93, no. 4, p. 045003, 2021.
\bibitem{Latorre2005} J. I. Latorre, ``Image compression and entanglement,'' arXiv:quant-ph/0510031, 2005.
\bibitem{Han2018} Z. Han, J. Wang, H. Fan, L. Wang, and P. Zhang, ``Unsupervised Generative Modeling Using Matrix Product States,'' \textit{Physical Review X}, vol. 8, no. 3, p. 031012, 2018.
\bibitem{Fei2021} S. M. Fei, ``Compressed Sensing Based on Tensor Network Machine Learning,'' \textit{Physical Science \& Biophysics Journal}, vol. 5, no. 1, p. 16000174, 2021.
\bibitem{Guala2023} D. Guala et al., ``Practical overview of image classification with tensor-network quantum circuits,'' \textit{Scientific Reports}, vol. 13, no. 1, p. 4427, 2023.
\bibitem{Haga2024} A. Haga, ``Quantum annealing-based computed tomography using variational approach for a real-number image reconstruction,'' \textit{Physics in Medicine and Biology}, vol. 69, no. 4, p. 02155, 2024.
\bibitem{Zhai2025} X. Zhai, T. Xiao, J. Huang, J. Fan, and G. Zeng, ``Quantum neural compressive sensing for ghost imaging,'' \textit{Physical Review Applied}, vol. 23, no. 1, p. 014018, 2025.
\bibitem{Nau2023} M. A. Nau, A. H. Vija, W. Gohn, M. P. Reymann, and A. Maier, ``Exploring the Limitations of Hybrid Adiabatic Quantum Computing for Emission Tomography Reconstruction,'' \textit{Journal of Imaging}, vol. 9, no. 10, p. 221, 2023.
\bibitem{Craps2024} B. Craps, M. D. Clerck, O. Evnin, and P. Hacker, ``Integrability and complexity in quantum spin chains,'' \textit{SciPost Physics}, vol. 16, no. 2, p. 041, 2024.
\bibitem{MathematicalFramework2026} ``Mathematical Framework for HVK,'' internal technical report, June 2026.
\bibitem{Xu2021} R. Xu, X. Wang, K. Chen, B. Zhou, and C. C. Loy, ``Positional Encoding as Spatial Inductive Bias in GANs,'' in \textit{Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)}, 2021, pp. 13564--13573.
\bibitem{MethodologyExp2026} ``Methodology and Experimental Analysis of the Hamiltonian Vision Kernel,'' internal laboratory reference, 2026.
\bibitem{Araz2022} J. Y. Araz and M. Spannowsky, ``Classical versus quantum: Comparing tensor-network-based quantum circuits on Large Hadron Collider data,'' \textit{Physical Review A}, vol. 106, no. 6, p. 062423, 2022.
\bibitem{Slabbert2024} D. Slabbert and F. Petruccione, ``Hybrid Quantum-Classical Feature Extraction approach for Image Classification using Autoencoders and Quantum SVMs,'' arXiv:2410.18814, 2024.
\bibitem{Liu2022} Z. Liu, P.-X. Shen, W. Li, L.-M. Duan, and D.-L. Deng, ``Quantum capsule networks,'' \textit{Quantum Science and Technology}, vol. 8, no. 1, p. 015016, 2022.
\bibitem{Shi2022} J. Shi, W. Wang, X. Lou, S. Zhang, and X. Li, ``Parameterized Hamiltonian Learning With Quantum Circuit,'' \textit{IEEE Transactions on Pattern Analysis and Machine Intelligence}, vol. 45, no. 5, pp. 6086--6095, 2022.
\end{thebibliography}

\end{document}
